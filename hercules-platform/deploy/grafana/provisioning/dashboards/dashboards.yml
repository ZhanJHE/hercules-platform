# Grafana 大盘自动装配：扫描 /var/lib/grafana/dashboards 下的 JSON
apiVersion: 1

providers:
  - name: Hercules
    folder: Hercules
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
