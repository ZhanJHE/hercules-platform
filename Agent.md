# 此文档作为默认情况下的Agent规范文档，一切Agent操作必须遵循此文档：
1. 约束/规范：
- 禁止未经允许修改此文档，如果需要修改必须先向用户确认，且此文档本身禁止使用过多格式控制符号。
- 回答禁止使用表情符号，禁止使用修辞手法美化输出，语气风格要直白、严谨、正式、简洁明了，禁止过多地堆砌专业词汇而不解释。
- 禁止在回答或者比较时使用五分制/十分制等打分制的方案或者计划的评价，如有需要必须先明确每个方案或者计划的优缺点。
- 涉及到复杂计算问题必须使用计算脚本等方式解决，简单计算除外。
- 遇到环境或者网络等问题，必须停下来询问用户，禁止反复盲目测试。
- 修改工作目录以外的外部环境前必须向用户确认。如依赖、运行环境、系统设置等，同一阶段内的连续小操作可合并汇报。
- 每个新Agent/子代理必须先导入此文档进入上下文。

2. 关于文档
- 每个项目开始前，一定要向用户确认现在以及未来的开发文档如何划分，默认文档的名称和内容统一用中文，文档风格和回答风格/此文档保持一致，统一存在 [工作目录/开发文档/]下。
- 常见开发文档包含：数据库设计、模块设计、需求设计、测试文档、接口文档、工作日志/会话记录、路线图、当前问题记录、环境与部署等，可以根据具体项目情况增减文档。
- 除此以外默认生成一份[项目讲解.html]，利用html格式更清晰、更详细地向用户讲解项目的各个方面：
- 日期使用 YYYY-MM-DD 格式统一
[项目讲解.html]：
默认采用左侧切换页栏，右侧为可滚动翻页的讲解内容，可以通过更美观图形化、结构化的网页向用户讲解项目，左侧每一个可切换页可以对应一份开发文档，切换页数可以根据具体项目情况增减。

3. 版本控制
- 默认使用Git作为版本控制，采用三段式版本号，根据路线图/问题修复进行版本号更新。
- 默认不传Github仓库，除非用户要求。
- 不要把敏感信息打包到Git中或者上传到Github，例如API-Key、密钥配置、密码配置、用户个人信息等。
- 大文件、实验原始数据、硬件固件等单独存储，不传Git。
- 提交信息简洁说明变更。
- 上传Github则默认编写一份README，遵循Github默认风格。

4. 前端设计（软件开发）
根据具体项目决定前端样式设计思路，默认有以下两种模版：
方案一：采用左侧切换页栏，从上至下依次为用户事务的流程驱动设计。
方案二：采用上侧切换页栏，从左至右依次为用户事务的流程驱动设计，适用于功能复杂，非单一流程的情况。

5. 默认工作流
- 大致遵循设定计划、编写代码、测试、撰写相关文档并更新Git、找出问题或进行路线图下一步的循环，每一工作阶段之间必须汇报并由用户确认，同一阶段内的连续小操作可合并汇报。
- 工作流细节根据每个项目的不同可以定制化设计；

6. 编码规范
- 默认编写一键运行的脚本方便用户运行测试，命名为[start.sh]或[start.cmd]。
- 遵守所用语言的标准的代码和注释规范