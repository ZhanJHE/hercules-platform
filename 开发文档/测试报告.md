# 测试报告

| 项 | 内容 |
| :--- | :--- |
| 被测版本 | `hercules-platform` @ commit `01b7f09`（阶段 A+ 高可用加固后） |
| 测试日期 | 2026-09-02 ~ 2026-09-07（遗留事项清理追加于 2026-09-09） |
| 环境 | Windows / JDK 25（目标 21）/ MySQL 8 @3306 / Redis @6379（演练用黑洞桩与 TCP 转发器隔离真实实例） |
| 结论 | **通过**。自动化 46/46 →（认证后）53/53 →（遗留事项清理后）**67/67** 全绿，真机功能冒烟与故障演练全部符合预期 |
| 阶段 H-4 压测 | **通过**（2026-09-07 追加，500 并发阶梯，详见 §六） |
| 遗留事项清理 | **通过**（2026-09-09 追加，6 项修复 + 14 个新用例，详见 §七） |

---

## 一、自动化测试（清理前基线：46 通过；认证后 53；清理后 67，`mvnw test`，不依赖外部中间件）

> 下表为清理前（46 例）的逐类明细。认证阶段新增 AuthFlowTest 6 例与冒烟 401 用例（→53）；
> 遗留事项清理新增 CacheKeysTest 4 例、HerculesSyncPropertiesTest 3 例、消费者乐观锁 2 例、
> GlobalExceptionHandlerTest 2 例、冒烟 3 例（重复选课/退选重选/分页边界，→67）。全量明细见 §七。

| 模块 | 测试类 | 数 | 覆盖点 |
| :--- | :--- | ---: | :--- |
| hercules-cache | MultiLevelCacheManagerTest | 8 | 三级读路径回源/回填、L1 失效后 L2 回填、双级失效、命中率计算、**50 线程击穿合并 loader 仅执行 1 次**、leader 失败透传且重试 |
| hercules-cache | SingleFlightTest | 3 | 20 线程同键合并为 1 次回源、跨键隔离、失败透传且不缓存失败 |
| hercules-cache | CaffeineLocalCacheManagerTest | 4 | 逐键 TTL 过期、续写重置过期、默认 TTL 存活、invalidate 清覆盖登记 |
| hercules-cache | ResilientDistributedCacheManagerTest | 3 | 失败率达阈值→OPEN→降级零阻塞→半开探测→CLOSED；慢调用熔断；delete 重试 1 次后降级 |
| hercules-cache | CacheLoadLockTest | 3 | SET NX 互斥、Lua 令牌释放后可再抢占、Redis 异常按未抢到处理 |
| hercules-sync | VectorClockTest | 9 | 自增、逐分量 max 合并、空时钟被支配、相等、不相交/部分重叠并发、JSON 往返、空输入 |
| hercules-sync | VersionChangeConsumerTest | 5 | 无存量→插入应用、被支配→丢弃、支配→版本+1、并发→LWW 合并+时钟并集+冲突计数、合并后失效 L1 |
| hercules-sync | FieldLwwMergeStrategyTest | 5 | null 旧值直返、新/旧时间戳胜负、单侧字段补齐、同值不动 |
| hercules-application | HerculesApplicationTests | 6 | 端到端冒烟（见下） |

## 二、真机功能冒烟（连本机 MySQL/Redis，演示脚本六步）

| # | 场景 | 结果 |
| :--- | :--- | :--- |
| 1 | 列表查询首次/二次 | total=50；`dbLoad=1` → 二次 `l1Hit+1` |
| 2 | 失效 L1 → L2 回填 | `l2Hit=1`，`dbLoad` 不变 |
| 3 | 选课 | enrolled 87→88（事务提交后同步链刷缓存） |
| 4 | 模拟 node-sim 并发冲突 | courseName 合并、enrolled 保留，`conflict=1` |
| 5 | 退课 | 产生支配版本，回落 87/原名 |
| 6 | 指标端点 | `/actuator/prometheus` 暴露 `hercules_cache_*` / `hercules_sync_*` |

多模块重构后（阶段 A）复验通过；阶段 A+ 新增验证：请求体缺字段返回 **400**（@Valid 生效）。

## 三、故障演练（阶段 A+，模拟 Redis 挂起/恢复）

| 阶段 | 操作 | 观测结果 |
| :--- | :--- | :--- |
| 故障注入 | 应用指向黑洞监听器（接受连接永不响应） | miss 路径首请求 3.4s（建连+命令超时的有界代价） |
| 熔断生效 | 连续 miss 请求 | 熔断 OPEN 后延迟 **7-8ms**（对比无熔断时 60s 级挂起）；`redisDegraded` 持续累计 |
| 击穿防护 | 15 同键并发 | `dbLoad=1`（单飞合并） |
| 故障期写路径 | 选课 | 成功，256ms（DB 事实源，L2 写入降级跳过） |
| 恢复 | 黑洞换 6390→6379 转发器 | 10s 后半开探测成功，2 次探测后 **CLOSED**；L2 经真实 Redis 回填（`l2Hit=1`，8ms） |

## 四、遗留问题（2026-09-09 清理后更新，详见《工作日志》）

1. ~~无重复选课约束~~ —— **已修复**（§七：应用层守卫 + `uk_student_course` 唯一键 + 条件复活更新）；
2. ~~`page/size` 无边界校验~~ —— **已修复**（§七：page≥1，1≤size≤100，越界 400）；
3. ~~`CacheVersion` upsert 多实例需乐观锁~~ —— **已修复**（§七：`updateVersionRow` 乐观锁 + 唯一键冲突重查重试）；
4. ~~500 响应携带原始异常消息~~ —— **已修复**（§七：固定文案，堆栈仅落日志）；
5. 冲突合并旧值取自 L2，L2 逐出后退化为整体采用新值 —— 设计级限制，保留记录；
6. 关键字大小写分裂缓存键 —— **已修复**（§七：键小写归一，DB 排序规则大小写不敏感故语义不变）。

## 五、结论

当前版本功能、一致性同步链、高可用防护均达到设计预期，测试准入。

## 六、阶段 H-4：阶梯压测（2026-09-07，经网关，WSL2 Docker 五容器栈）

### 6.1 环境与方法

| 项 | 内容 |
| :--- | :--- |
| 被测链路 | JMeter → gateway:8081（JWT 鉴权 + traceId）→ app:8080 → MySQL 8 / Redis 7（容器栈，见《环境配置与部署》§二） |
| 压测机 | 同宿主 Windows，JMeter 5.6.2 无头（JVM 2g），双线程组 **读 70% / 写 30%** |
| 流量明细 | 读组每迭代 2 请求（课程列表 + 随机课程详情），写组每迭代 1 请求（选课 POST）；实际请求比约 94:6（写迭代更慢所致，如实记录） |
| 数据准备 | 账号 st001（token 每级重新登录）；压测课程 5000 门（容量 10 万/门，写路径无 409 干扰） |
| 观测 | JMeter HTML 报告（分位数/吞吐）+ Grafana 实时大盘（QPS/P95/L1/L2 命中/熔断/JVM/Tomcat） |
| 中止线 | 错误率 >1% 或 P95 异常飙升即中止（未触发） |

### 6.2 阶梯结果

| 级 | 并发（读/写） | 稳态 | 总请求 | 吞吐 req/s | 均值 | P95 | 错误 |
| :--- | :--- | :--- | ---: | ---: | ---: | ---: | :--- |
| 冒烟 | 5（3/2） | 15s | 10,952 | 728 | 6ms | — | 0 |
| 冒烟 | 20（14/6） | 60s | 208,412 | 3,470 | 5ms | — | 0 |
| L1 | 100（70/30） | 3min | 729,677 | 4,051 | 22.6ms | 106ms | **0** |
| L2 | 300（210/90） | 5min | 1,390,183 | 4,630 | 61.5ms | 249ms | **0** |
| L3 | 500（350/150） | 10min | 3,018,549 | **5,027** | 96.9ms | 170ms | 20（0.0007%） |

L3 分端点：

| 端点 | 请求 | 错误 | 均值 | P90 | P95 | P99 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: |
| read-list | 1,440,898 | 0 | 72ms | 107ms | 126ms | 300ms |
| read-detail | 1,440,730 | 0 | 70ms | 104ms | 119ms | 155ms |
| write-enroll | 136,921 | 20 | 641ms | 1147ms | 1651ms | 2239ms |

### 6.3 瓶颈定位与分析

1. **吞吐平台化于 ~5,000 req/s**：并发 100→300→500，吞吐 4,051→4,630→5,027 增益递减，均值延迟 23→62→97ms 近线性抬升——系统进入饱和区；
2. **写路径瓶颈：Hikari 连接池饱和**。L3 期间应用日志实证：`HikariPool-1 - Connection is not available, request timed out after 3000ms (total=20, active=20, idle=0, waiting=116)`——150 写并发下 20 连接耗尽，排队超 3s 的请求按阶段 A+ 全链路超时设计**快速失败**（500），而非无限堆积拖垮 Tomcat 线程池（20 次失败全部为此因，无超时挂起、无错误扩散）；
3. **读路径稳如磐石**：多级缓存扛住约 4,800 读/s（两读端点合计），0 错误、P99 ≤300ms——L1/L2 命中路径在满压下未回源劣化，缓存设计目标达成；
4. **口径声明**：压测机与被测栈同宿主（Windows + WSL2 VM），~5,000 req/s 处 JMeter 客户端与宿主资源都可能参与封顶；本数据用于阶梯对比与瓶颈定位，非集群化绝对容量上限；
5. **改进方向**（衔接路线图）：写路径削峰治本是**阶段 B 的 RocketMQ 异步化**（选课事务只落 DB，版本事件异步分发，削平同步链瞬时压力）；工程优化项含 Hikari 池参数调优、JDBC `rewriteBatchedStatements`。

### 6.4 压测工具踩坑记录（过程详见《工作日志》2026-09-07）

| 问题 | 处置 |
| :--- | :--- |
| Once Only Controller + 配置元素触发 `recoverRunningVersion` 无限递归（StackOverflowError），线程死亡 | 移除 Once Only 结构 |
| Header Manager 的线程变量 `${token}` 求值时机不稳 → 全量 401 | 登录移交启动脚本，Header 读全局属性 `Bearer ${__P(token)}` |
| `-J` 参数经 PowerShell 未展开（字面量 `$Threads` 直达 JMeter → 0 线程） | 全参数走 `-q` 属性文件，命令行零 `-J` |
| ThroughputController perThread 语义导致线程一轮即停（每线程恰 3 采样） | 弃用吞吐控制器，改双线程组建模 |
| 最终形态 | JMX 仅含最常用元素（双线程组 + HeaderManager + 3 个 HTTP 采样器），计划内零脚本零冷门控制器，一次通过 |

### 6.5 单机共置口径与 CPU 抢占分析（JMeter 与被测同宿主）

**结论：共置做法合理，但必须区分两类结论的有效性。**

1. **仍然有效**的结论：阶梯趋势与相对比较（同环境下 A/B 对比双方承受同样抢占）；瓶颈定位（Hikari 饱和是服务端日志证据，与抢占无关）；错误率行为验证（3s 快速失败设计）；缓存/超时等机制性验证；
2. **需标注口径**的结论：绝对吞吐（5,027/s）是"这台机器共置形态"的数字，含 JMeter 客户端与宿主调度封顶，不能外推为服务端集群容量；
3. **量化证据**（调优后 L3 期间容器 CPU 采样，30s 粒度）：app 稳态 **~4.3/12 逻辑核（36%）**、gateway ~2.3 核、mysql ~1.3 核——**容器合计 ~8 核，VM 尚有余量，服务端并非 CPU 饱和**；基线 5,027/s 平台化的封顶方不在 app CPU（嫌疑为 JMeter 客户端 GC/调度与网关侧）；
4. **宿主事实**：i5-12400F 仅 6 物理核/12 逻辑核，WSL2 默认占满 12 逻辑核 + 16GB，JMeter 与 VM 共争物理核——抢占客观存在，但"占满逻辑核"≠"服务端 CPU 瓶颈"，以容器侧实测为准；
5. **改进选项**：本期已做（JMeter 加堆 + ParallelGC 降客户端 GC 频率；容器显式 mem_limit 隔离邻居效应）；可选进阶：`.wslconfig` 限制 WSL2 核数 + JMeter `Start-Process -ProcessorAffinity` 绑定其余核（需 WSL 重启）；展望：第二台压测机/云主机分离流量发生器。

### 6.6 JVM 参数与连接池调优 A/B 对比（L3 同参数复跑）

**变更清单**（多变量同时变更，解读时注意）：

| 对象 | 基线 | 调优后 |
| :--- | :--- | :--- |
| app JVM | 全默认（自适应 Xms250M/**Xmx4G**） | Xms1g/Xmx2g + AlwaysPreTouch + G1 MaxGCPauseMillis=50 + GC 日志 |
| gateway JVM | 全默认（自适应 Xmx4G） | Xms256m/Xmx512m + AlwaysPreTouch |
| 容器限额 | 无（cgroup=max） | app 3g / gateway 1g |
| Hikari 池 | 20（L3 实证饱和 waiting=116） | **40** |
| JMeter 客户端 | Xms1g/Xmx2g + G1 | Xms1g/Xmx4g + ParallelGC |

**结果对比**（500 并发/10min 同参数）：

| 指标 | 基线 | 调优后 | 变化 |
| :--- | ---: | ---: | :--- |
| 总吞吐 | 5,027/s | 4,317/s | -14% |
| 错误 | 20（Hikari 超时） | **0** | ✓ 写路径可靠性根治 |
| 写吞吐 | 228/s | **424/s** | **+86%** |
| 写均值 / P95 / P99 | 641 / 1651 / 2239ms | **345 / 513 / 636ms** | **-46% / -69% / -72%** |
| 读吞吐（两端点合计） | 4,802/s | 3,894/s | -19% |
| 读均值 / P95 | ~70 / ~120ms | ~88 / ~145ms | 回退 |
| app GC | 未记录 | 695 次 Young / **0 Full** / avg 6.5ms / P95 12.8ms / 总耗时 **0.76%** | app GC 非瓶颈 |

**解读**：

1. **Hikari 20→40 是明确收益**：写吞吐 +86%、写延迟近乎减半、20 个连接超时错误清零——直接对症 §6.3 定位的瓶颈；
2. **读路径回退 -19% 无法归因于 app GC**（实测仅占 0.76% 时间、0 Full GC）；嫌疑集中于 JMeter 客户端（ParallelGC 4g 堆在共享 6 物理核上的 STW 行为）与 gateway 512m 堆的 GC 频率——**列为后续单变量实验**（逐项回退对比），本报告如实记录多变量方法局限；
3. 总吞吐 -14% 主要由读路径回退贡献；写路径的确定性收益独立成立；
4. **结论**：JVM 层确有调优空间但非免费午餐——可靠性收益明确（错误清零）、写路径收益明确、吞吐回退待单变量归因；参数已固化进 compose 与压测脚本，GC 日志持续落盘供论文取证。

## 七、遗留事项清理（2026-09-09 追加）

### 7.1 修复清单（6 项）

| # | 修复 | 方案 |
| :--- | :--- | :--- |
| 1 | 重复选课无约束 | `t_enrollment` 增加 `uk_student_course(student_id, course_id)` 唯一键（每对学生-课程一行，状态原地流转）；`enroll` 前置守卫（已有 status=1 → 409）→ 条件 UPDATE 原地复活退选行（0 行 → 409 回滚扣减）→ insert 捕获唯一键冲突转 409（并发双击兜底）。**语义变化**：退选后再选不再新增行，历史流水折叠为单行状态 |
| 2 | 分页无边界 | `page ≥ 1`、`1 ≤ size ≤ 100`，越界 400（显式拒绝，不再放行单页拉全表） |
| 3 | 版本表 upsert 竞态 | `CacheVersionMapper.updateVersionRow` 乐观锁更新（`WHERE current_version = 期望值` 且 `+1`）；insert 唯一键冲突重查转更新；乐观锁失败重查重试一次，仍失败跳过本次时钟持久化（缓存已应用，下条消息重新决策） |
| 4 | 500 响应泄漏细节 | 兜底分支固定文案「服务内部错误，请稍后重试」，原始异常消息只落 ERROR 日志（traceId 可回溯）；业务异常文案不受影响 |
| 5 | sync 配置/监听器健壮性 | `maxNodes < 1`、`nodeId` 空白在 setter 即抛 IllegalArgumentException（拒绝启动）；事件监听器 null 事件/载荷前置判空（原 catch 块内取 key 自身会二次 NPE） |
| 6 | 批量播种慢（57s/5000 行） | JDBC URL 追加 `rewriteBatchedStatements=true`（compose 与本机配置同步修改，H2 测试 URL 不动） |

### 7.2 顺带修复的隐藏编译错误

`GlobalExceptionHandler` 对 `HttpMediaTypeNotSupportedException` 的 import 写为 `org.springframework.http.converter`（该包位置从未存在，此类一直位于 `org.springframework.web`，全部 spring-web 5.3~7.0 jar 实证）。H-4 期间引入后未再执行干净编译，本次清理首次全量重编译暴露并修正——**此前所有"全绿"结论的有效性不受影响（该错误仅在干净编译时暴露），但已记入工作日志作为流程教训**。

### 7.3 新增用例（14 个，53 → 67）

| 模块 | 测试类 | 新增 | 覆盖点 |
| :--- | :--- | ---: | :--- |
| hercules-common | CacheKeysTest | 4 | 详情键格式、空关键词占位、trim+大小写归一（"Java"/"java" 同键）、":"与"%"转义回归 |
| hercules-sync | HerculesSyncPropertiesTest | 3 | maxNodes 正数校验、nodeId 非空白校验、默认值合法 |
| hercules-sync | VersionChangeConsumerTest | +2 | insert 唯一键冲突→重查转乐观锁更新；乐观锁两次竞争失败→跳过持久化不抛异常 |
| hercules-application | GlobalExceptionHandlerTest | 2 | 500 固定文案且不含内部细节；业务异常文案保留（对照） |
| hercules-application | HerculesApplicationTests | +3 | 重复选课 409（守卫先于容量）；退选后再选原地复活同一行（id 不变）+ /mine 单条；分页越界 400/边界 200 |

> 既有冒烟 Order 6 同步改写：课程 39 的 3 个余量由 st001~st003 各选一门填满（唯一键下每人限选一门），
> 第 4 名学生（st004，test profile `demo-student-count: 4`）触发 409 容量已满——与重复选课 409 形成可区分的两条路径。

### 7.4 结果

- `mvnw test`：**67/67 全绿**（common 4 + cache 21 + sync 24 + application 18）；
- 数据库变更仅 H2 测试库自动生效；存量 MySQL 环境需手动执行 schema.sql 头部注记的一次性 ALTER。

## 八、阶段 B：真实 Binlog 同步链验证（2026-09-10 追加）

### 8.1 链路与验证方法

| 项 | 内容 |
| :--- | :--- |
| 链路 | MySQL ROW Binlog → canal-server 1.1.7（MQ 模式投递 flatMessage，partitionHash 按 表+主键 保序）→ RocketMQ 4.9.4（topic `hercules-binlog-topic`，8 队列）→ 应用 `RocketMQBinlogListener` → `BinlogToVersionConverter` → `VersionChangeConsumer`（判定/合并/落版本表逻辑复用，零改动） |
| 传输开关 | `hercules.sync.transport=canal-mq`（容器栈）/ `in-process`（默认，单机/测试） |
| 观测 | 应用日志（binlog applied 行携带 traceId）、`/api/v1/cache/stats`（versionApplied/conflictDetected）、`mqadmin topicStatus` |

### 8.2 容器端到端实测（九容器栈）

| # | 场景 | 结果 |
| :--- | :--- | :--- |
| 1 | 登录 → 选课（事务只落库）→ 轮询详情缓存 | **617ms** 同步到新 enrolled（87→88，含 100ms 轮询粒度），< 1s 验收线 |
| 2 | **外部直改数据库**（docker exec UPDATE，完全绕开应用）→ 轮询详情 | **250ms** 同步新 course_name；中文经链路 UTF-8 无损（SQL 文件方式写回 73ms 验证） |
| 3 | canal 重启（位点持久化）→ 退课 | **199ms** 同步；applied 仅 +1（无位点重放）、conflict=0（无虚假冲突） |

### 8.3 单元测试增量（67 → 79）

| 测试类 | 数 | 覆盖点 |
| :--- | ---: | :--- |
| sync/BinlogToVersionConverterTest | 6 | flatMessage 解析、行→Course→VersionedValue（键/时钟存量⊕自增/es 时间戳）、多行逐条发布、非关注表与 DDL 跳过、DELETE 忽略、存量时钟因果链保持、非法 JSON 透传 |
| sync/CourseRowMapperTest | 4 | DATETIME(3) 三格式、数值宽松解析（非法置 null 不进合并）、id 缺失返回 null |
| sync/HerculesSyncPropertiesTest | +2 | transport 取值校验（非法拒绝启动）、rocketmq 连接项校验 |

### 8.4 环境问题记录（排障过程见《工作日志》）

1. canal 账号认证 1045：MySQL 8 默认 `caching_sha2_password`，canal 1.1.7 自研解析驱动不支持 → 改 `mysql_native_password`；
2. canal → RocketMQ 投递 NPE（`CanalRocketMQProducer.send:239`，字节码定位为 `partitionsNum` 拆箱）：`canal.mq.partitionsNum` 必须显式配置 → 设为 8（与 topic 队列数一致）；
3. docker exec 传中文 SQL 经 PowerShell 按 GBK 字节写入导致乱码 → 属测试操作编码问题，链路本身 UTF-8 无损（SQL 文件 + `--default-character-set=utf8mb4` 验证）。

### 8.5 结论

阶段 B 通过：真实 Binlog 链路端到端延迟均 < 1s，外部直改库同步闭环（答辩演示点），消费端判定/合并/版本表逻辑与进程内形态完全复用。

## 九、阶段 D：多智能体对话链路验证（2026-09-10 追加）

### 9.1 链路与验证方法

| 项 | 内容 |
| :--- | :--- |
| 链路 | 前端/客户端 → 网关（JWT 验签 + traceId）→ ChatController（SSE）→ AgentOrchestrator → IntentRouter（规则优先 + GLM 兜底）→ 智能体链（推荐 = 候选检索 + 冲突标注 + LLM 流式；选课 = 纯规则校验 + 确认制执行）→ t_agent_trace 落库 |
| LLM | 智谱 GLM glm-4.5-air（OpenAI 兼容 /api/paas/v4，Spring AI 1.0.6），Key 经 gitignore 的 local yml / deploy/.env 注入 |
| 测试隔离 | agent/application 全部自动化测试使用 StubLlmPort（脚本化响应）@Primary 覆盖，零网络依赖 |

### 9.2 容器端到端实测（九容器栈，经网关 8081）

| # | 场景 | 结果 |
| :--- | :--- | :--- |
| 1 | 学生登录 → 「推荐几门 3 学分的课」→ SSE 流式 | **18s** 完成（GLM thinking 首 chunk ~18s 属模型特性）；token 流 + meta（candidateCount=8，含先修/冲突标注） |
| 2 | 「帮我选 CS101」→ 冲突校验通过 → 待确认 → 「确认」→ 真实选课 | 校验/确认两步即回（规则计算 0s）；enrollmentId 落库；**binlog 链刷新详情缓存 enrolled=1** ✓ |
| 3 | 无效 GLM Key（故障注入）→ 推荐请求 | **1s 降级**：返回结构化候选列表（不 5xx、对话不中断） |

### 9.3 单元测试增量（79 → 96）

| 测试类 | 数 | 覆盖点 |
| :--- | ---: | :--- |
| agent/RoutingAgentTest | 4 | 关键词规则优先级（CONFIRM>ENROLL>RECOMMEND）、LLM 兜底 JSON 宽松解析、LLM 故障降级 RECOMMEND、课程线索提取（id/编码/书名号/剥离动词） |
| agent/SchedulingAgentTest | 4 | 时间片重叠（同天节次交集）、先修 ID 校验、学分合计、脏 JSON 与同课程容忍 |
| agent/AgentOrchestratorTest | 5 | 推荐链流式+meta+trace、降级形态、选课确认制两步流转、冲突拒绝不设待确认、非学生执行拒绝 |
| application/ChatFlowTest | 4 | 未登录 401、推荐 SSE 贯通（token/meta 事件）、选课确认制安全边界（非学生执行拒绝）、会话历史端点 |

### 9.4 环境问题记录（详见《工作日志》）

1. **存量库压测残留数据拖垮智能体链**：`/enrollment/mine` 返回 514,844 行（H-4 压测期无守卫重复插入）→ 清理压测选课记录 + 去重 + 补加 `uk_student_course` 唯一键 + 按有效记录校准 enrolled（遗留事项闭环）；
2. **SSE 异步转发与 Security 冲突**：ASYNC dispatch 重跑过滤器链而自定义 JWT 过滤器（OncePerRequest）被跳过 → 匿名拒绝叠加已提交响应 → `spring.security.filter.dispatcher-types=request,error` 限定；
3. **先修课比对口径修正**：prerequisites_json 实为课程主键 ID 列表（起步文档 §5.1.1），初版按编码比对永不满足 → 改按 ID 比对；
4. GLM thinking 模型响应 9~19s（首 chunk 慢）→ 流式输出承接，max_tokens 需覆盖思考+正文。

### 9.5 结论

阶段 D 通过：自然语言推荐 / 冲突校验 / 确认制选课端到端贯通，LLM 故障 1s 降级、写路径安全边界（确认制 + STUDENT + 越权收敛）全部生效，智能体链路 traceId 可追踪。

## 十、阶段 G：Vue3 前端验证（2026-09-10 追加）

### 10.1 范围与方法

| 项 | 内容 |
| :--- | :--- |
| 范围 | hercules-ui：登录、课程大厅、课程详情、我的选课、对话助手（SSE 流式）、治理驾驶舱、课程管理、演示工具；左侧菜单 + 顶栏布局 |
| 自动化 | Vitest 15/15：SSE 解析器（粘包/多行/CRLF/finish）、401 单飞刷新（并发合并/失败登出/无 token）、JWT 解码（UTF-8/非法）、学分十进制合计；`vue-tsc` 类型检查 + `vite build` 零错误 |
| 端到端 | 十容器栈（nginx:8090 → 网关 8081 → 应用 → GLM），HTTP 层逐项实测（等价于浏览器网络路径） |

### 10.2 容器端到端实测（经 nginx:8090）

| # | 场景 | 结果 |
| :--- | :--- | :--- |
| 1 | 静态托管（SPA 入口） | HTTP 200，应用挂载点正常 |
| 2 | 登录（/api 反代 → 网关 → 应用） | accessToken 正常签发 |
| 3 | 对话推荐（SSE 经 nginx 反代） | **156/156 token 事件流式到达**（缓冲已关闭），meta 结构化数据完整，全文 11s（GLM thinking 首包特性） |
| 4 | LLM 故障降级（无效 Key 注入） | 1s 返回结构化候选列表 |

### 10.3 排障记录（详见《工作日志》）

1. GLM thinking 吃满 max_tokens → 正文为空（token 事件 0）：max_tokens 1024→4096 + 编排器空响应自动补发候选列表 + 修复 onErrorResume 与 concat 兜底重复；
2. LLM 超时 30s 误杀正常生成（thinking 首包 18~32s）→ 放宽至 90s；
3. 容器内 npm ci 受限网络超时（600s+）→ 镜像改为宿主预构建 dist + nginx 单阶段；
4. MockMvc/SSE 断言：getContentAsString 需显式 UTF-8；token 分片切断中文，断言需拼接 data 行。

### 10.4 浏览器人工冒烟清单（待项目所有者执行）

十条路径：登录 → 课程大厅搜索/分页 → 详情 → 选课确认 → 409 文案 → 我的选课/退课 → 对话推荐流式 → 候选卡选课 → 确认 → 驾驶舱轮询与演示工具。HTTP 网络路径已全部实测通过，浏览器交互体验由所有者验收。

### 10.5 结论

阶段 G 通过：前端全页面接入真实后端（零 mock），SSE 流式经 nginx/网关全链路无缓冲，认证静默刷新与安全边界经单测覆盖。浏览器交互体验待所有者最终验收。
