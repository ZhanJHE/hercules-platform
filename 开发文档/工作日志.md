# 工作日志

> **项目**：智慧校园多智能体服务平台（Hercules）——MVP：分布式缓存一致性治理核心
> **记录规则**：按日期条目记录当日完成事项、遇到的问题与处置、产出物清单；「已知事项」小节累积备注（答辩备查 / 后续待办）。

---

## 2026-09-02（周三）

### 1. 起步文档研读与 MVP 方案规划

- 通读《起步文档.md》（12 周迭代计划、四层架构、技术选型、数据库设计、风险清单）与《设计类.md》（Java 类架构蓝图：包结构树 + 7 张 mermaid 类图）。
- 核实本机环境：JDK 25（编译目标仍设 21）；**MySQL 8（3306）与 Redis（6379）已在本机运行**；Docker 未安装；Maven 未安装（使用 wrapper 3.9.16）；`~/.m2` 无镜像配置（直连中央仓库），本地仓库已有 531MB 缓存。
- 与项目所有者确认三项决策：
  1. **MVP 范围 = 纵向切片**：课程查询走多级缓存 + 选课写库 + 进程内向量时钟同步链；不引入 Canal/RocketMQ/LLM；
  2. **Spring Boot 骨架降级**：4.1.1 → 3.5.16（与起步文档选型生态 MyBatis-Plus / Spring AI Alibaba 兼容）；
  3. **单模块分包起步**：`com.zhanjh.hercules.*` 同构于目标多模块，Sprint 2 再拆分。
- 产出《最小可执行方案》：7 步实施、6 条验收标准（DoD）、预估 4~4.5 人日。

### 2. MVP 开发（纵向切片）

- **构建前提**：核实 Spring Boot 3.5.x 最新补丁 3.5.16、MyBatis-Plus 3.5.17 均存在于 Maven Central。
- **工程与配置**：pom 降级并补齐依赖（web/validation/actuator/data-redis/mybatis-plus/mysql-connector-j/caffeine/micrometer-registry-prometheus/test+h2）；`application.yml` + `application-local.yml`（真实凭据，已 gitignore）；`schema.sql`（t_course / t_enrollment / t_cache_version 建表 + t_repair_log / t_agent_trace 占位表）；`data.sql`（50 门课程、20 名教师种子，幂等 INSERT IGNORE）。
- **缓存内核**（对应 FR-HC-01）：`MultiLevelCacheManager`（L1 Caffeine → L2 Redis → MySQL，cache-aside 逐级回填，L2 允许缺省）、`FixedTTLStrategy`（列表键 10s 最终一致窗口 / 详情键 L1 60s、L2 300s）、`CacheStatsCollector`（命中统计 + Micrometer FunctionCounter 指标族）。
- **向量时钟同步链**（对应 FR-HC-03，核心创新点）：`VectorClock`（increment / merge / compare / isConcurrent，TreeMap + synchronized）；`VersionedValue` 载荷；进程内事件总线 `InProcessEventBusProducer` + `@TransactionalEventListener(AFTER_COMMIT)` 保证「先提交、后同步」；`VersionChangeConsumer` 决策表——AFTER 应用（写 L2 + 失效 L1 + upsert 版本记录）、EQUAL/BEFORE 幂等丢弃、CONCURRENT 走 `FieldLwwMergeStrategy` 字段级 LWW 合并；向量时钟持久化到 `t_cache_version.vector_clock_json`。
- **API 层**：课程分页/详情、选课（事务内防超选条件 UPDATE）/退课、缓存统计、答辩演示钩子（`POST /api/v1/debug/simulate-conflict`、`/evict-local`）。
- **中途修复的代码问题**：
  1. MyBatis-Plus 3.5.9+ 将分页插件拆分至独立模块 → 补 `mybatis-plus-jsqlparser` 依赖（编译错误消除）；
  2. 测试类型与断言问题 2 处（cacheHitRate 需 double 断言；`l2Miss` 两次未命中应计 2）。

### 3. 构建与验证

- `mvnw test`：**31/31 全绿**（VectorClock 9、FieldLwwMergeStrategy 5、MultiLevelCacheManager 6、VersionChangeConsumer 5、端到端冒烟 6——含防超选 409 与并发合并断言）。
- 真机运行冒烟（连本机 MySQL + Redis，全程无 Docker），演示脚本六步全部符合设计：
  1. 列表首次 `dbLoad=1` → 第二次 `l1Hit=1`；
  2. 失效 L1 → `l2Hit=1` 回填、`dbLoad` 不变；
  3. 选课 enrolled 87→88（事务提交后同步链自动刷缓存）；
  4. node-sim 并发冲突合并：courseName 更新、enrolled 保留，`conflict=1`；
  5. 退课产生支配版本覆盖合并值（87、原名）；
  6. `/actuator/prometheus` 暴露 `hercules_cache_*` / `hercules_sync_*` 指标。
- **环境问题与处置**（按约定：停下询问/系统排查，不盲目重试）：
  1. Maven wrapper 首次运行需写 `~/.m2/wrapper/dists`，被工作区写沙箱拒绝 → 按沙箱规则一次性提权重试，经所有者审批后通过；
  2. `Start-Process` 启动的应用进程随前台命令结束被沙箱回收（health 曾短暂 UP 后连接被拒）→ 改用**后台任务**常驻运行，验证完成后 `job_kill` 停止；
  3. mermaid.ink 渲染返回 400 → 编写探针脚本对比 4 种编码格式：原始代码 base64 ✅、JSON state base64 ✅、deflateRaw/pako ❌（400），据此修正渲染脚本（环境连通正常，属请求格式问题）。

### 4. Javadoc 中文注释工程（五路并行，进行中 → 已完成，见补记）

- 统一规范：类/接口/枚举/Record 顶部 Javadoc（首句定位 + `<p>` 分段机制说明 + `@author zhanjh` + `@since 0.0.1`）；方法级 `@param/@return/@throws`；逐字段注释（业务含义、默认值、对应表列/配置键）；行内 `//` 注释解释「为什么」；**只允许改注释、严禁改代码**。
- 分工与进度：sync（12 文件）✅、common/model/mapper（12 文件）✅、service/controller/config（8 文件）✅、cache（11 文件）✅（补记）、test（6 文件）✅（补记），共 **49 文件**。
- 各路均回报「代码零改动」核验方式（剥离注释逐行比对 / 逐行抽查）。
- 完成后统一执行 `mvnw test` 回归（结果见补记）。

### 5. 开发文档归档

- 新建 `开发文档/` 目录；`起步文档.md` 移入归档。
- `images/`：7 张类图 PNG（`extract_mermaid.py` 渲染，Python 3.11.9 标准库实现，经 mermaid.ink）——领域模型 / 缓存治理层 / 多智能体决策层 / RAG 检索层 / 巡检自愈引擎 / 可观测性 / API 网关层。
- 《包结构说明.md》：整体包结构树 + 11 个模块逐类详细注释（含 MVP 实现状态标注 ✅/🔄/⏳）、模块依赖矩阵、MVP 单模块 → 目标多模块映射表。
- 本日志《工作日志.md》建立。

### 已知事项与后续待办（注释工程发现汇总，答辩备查）

> 来源：五路注释任务的「只报告不修改」清单，按主题归并。

**API/校验类**
1. `@NotNull` 校验实际不生效：控制器参数未加 `@Valid`，缺 courseId 表现为 404「课程不存在： null」而非 400；缺 studentId 会落库 null（Sprint 2 收紧）→ **当日已修复**（见补记）。
2. 无重复选课约束：应用层不查已有 status=1 记录，`t_enrollment` 亦无唯一约束，可重复选课致 enrolled 与记录数失衡。
3. 列表缓存键未转义：keyword 含 `:` 时键段可能碰撞；"Java"/"java" 键分裂；page/size 无边界校验（size 大值可单页拉全表）→ **键碰撞当日已修复**（百分号转义）；大小写分裂与分页边界保留（不影响正确性）。
4. 500 响应携带原始异常消息，可能泄漏内部细节，上线前收紧。

**演示注意类**
5. 连续两次 `simulate-conflict`：第二次 `{node-sim:1}` 相对存量时钟为 BEFORE 被幂等丢弃——答辩演示前需先在 node-1 发生一次选课/退课。
6. `simulate-conflict` 不写 `t_course`，TTL 过期回源后课程名还原（演示窗口期注意）。

**并发/一致性类**
7. `CacheVersion` upsert 为 select-then-write 无锁：单实例（进程内总线串行驱动）安全；多实例需乐观锁或 `cache_key` 唯一键冲突重试。
8. `FieldLwwMergeStrategy` 为「字段并集 + LWW」叠加语义：新值时间戳更旧时，仅新值存在的字段仍会并入（严格 LWW 应整体保留旧值）——答辩追问时需备好说辞。
9. 冲突合并的旧值取自 L2：L2 被逐出时退化为整体采用新值，仅存于旧值的字段丢失（冲突路径已知盲区）。

**数据库类**
10. `enrolled`/`capacity` 仅靠条件 UPDATE + 应用层校验，无 DB CHECK 兜底。
11. JSON 列以 TEXT 承载（与起步文档 JSON 类型有偏差，Sprint 2 如需 JSON 函数再切换）。

**代码卫生类**
12. `VectorClock.fromJson` 的 merge 函数为防御性死代码（Jackson 解析阶段重复 key 已被后者覆盖）。
13. `STATUS_PRESELECT` 为无写路径的预留常量；`withdraw` 的 409 分支正常数据不可达（防御代码）。
14. `HerculesSyncProperties.maxNodes` 无下限校验；事件监听器对 null 载荷兜底不闭合。
15. **L1 列表键短 TTL 不生效**（cache 注释任务发现）：Caffeine 原实现忽略逐键 TTL 参数，列表键在 L1 按全局 60s 过期，「10s 最终一致窗口」仅 L2 生效 → **当日已修复**（自定义 Expiry，见补记）。
16. 测试健壮性备注：`concurrentVersionIsMergedWithLww` 的 LWW 胜负依赖系统时钟不回拨（正常恒成立）；`readLong` 的 JSON Pointer 转换（`"$.x"`→`"/x"`）经实测行为正确。

---

### 补记（当日 15:10~16:00，阶段 0 完成）

- Javadoc 注释工程五路全部完成（49 文件），统一 `mvnw clean test` 回归：**31/31 通过**，确认注释未破坏任何代码。
- 阶段 0 三项修复（对应已知事项 #1、#3、#15）：
  1. 控制器请求体补 `@Valid`，`@NotNull` 生效（字段缺失返回 400 而非 404/null 落库）；
  2. `CacheKeys.courseList` 关键词百分号转义（`%`→`%25`、`:`→`%3A`），杜绝列表键键段碰撞；
  3. `CaffeineLocalCacheManager` 重写为自定义 Expiry 逐键 TTL——**L1 的列表键 10s 最终一致窗口自此生效**；修复过程中测试抓出 `expireAfterRead` 返回 `Long.MAX_VALUE` 会被 Caffeine 理解为「读一次即永生」的语义错误，改为返回剩余时长（读不续期）。
- 修复后回归：**35/35 通过**（新增 `CaffeineLocalCacheManagerTest` 4 例）。
- 本地 git 仓库建立于工作区根（纯本地、无远端），完成基线提交；后续每阶段一个 commit。
- 下一阶段：多模块化重构（阶段 A）已获批启动。

## 2026-09-02（周三）· 阶段 A：多模块化重构

### 完成事项

1. **多模块骨架**：新建 `hercules-platform/`（父 POM 继承 spring-boot-starter-parent 3.5.16 + dependencyManagement 统一内部模块/mybatis-plus 版本），11 个模块：common / cache / sync / inspector / rag / agent / observability / admin / application / gateway（gateway 为独立进程，Spring Cloud BOM 预留注释，阶段 H 启用）。
2. **代码平移**（按包 1:1）：common+model+mapper → `hercules-common`；cache → `hercules-cache`；sync → `hercules-sync`；service/controller/config/启动类 → `hercules-application`（唯一可执行 jar）；resources（yml/schema/data/凭据）全部归 application。
3. **模块解耦修正**：
   - `GlobalExceptionHandler` 从 common 调整为应用层 `com.zhanjh.hercules.web` 包（common 不再依赖 spring-web），补显式 import；
   - `HerculesSyncProperties` 改由 sync 模块新增 `SyncConfig` 自注册，`CacheConfig` 不再跨模块 enable（消除 common←cache←sync 之外的耦合）；
   - 测试桩 `InMemoryDistributedCacheManager` 按需复制到 cache/sync/application 三个测试树（避免引入 test-fixtures 模块的额外复杂度，MVP 取舍）。
4. **构建验证**：reactor 全绿——cache 10、sync 19、application 6（H2 冒烟），共 **35/35**；`mvnw package` 产出可执行 jar（47MB）。
5. **真机冒烟复验**（连本机 MySQL/Redis）：列表 50 条；选课 course2 95→96（同步链生效）；node-sim 冲突合并（名称合并、enrolled 保留，conflict=1）；**@Valid 生效——缺 studentId 返回 400**（阶段 0 修复项真机确认）；退课回落 95。
6. **退役与归档**：旧 `hercules/` 目录删除（git 基线 `bfb4b6e` 保留历史）；README 重写迁移至 `hercules-platform/README.md`；仓库根新增 .gitignore（target/node_modules/凭据等）；Maven wrapper 复制到平台根。

### 本阶段修复的构建问题

- sync 测试缺 `InMemoryDistributedCacheManager`（平移时漏复制一份）；
- `GlobalExceptionHandler` 换包后 `R`/`BusinessException` 需显式 import（原同包免导入）。

### 结论

多模块化完成，功能与单模块版完全等价；后续阶段（B 真实同步链路 → C 巡检 → D 智能体 → E RAG → F 可观测 → G 前端 → H 网关/压测/部署）在对应空模块内填充即可，不动既有模块结构。

### 补充产出（阶段 A 后追加）

- **《开发文档/环境配置与部署.md》**：环境配置与运行部署双模式指南——
  - **模式一 非 Docker（✅ 当前生效）**：环境要求、MySQL/Redis 直装要点、配置分层（含 `application-local.yml` 模板与 `hercules.*` 参数表）、构建/运行/冒烟清单、数据重置、7 项故障排查；
  - **模式二 Docker（⏸ 规划，暂不执行）**：按阶段 B/E/F/H 的容器规划表（镜像/端口/关键配置）、compose profile 命令草案、与模式一的 `hercules.sync.transport` 切换关系、网络问题即停约定；落地时转为操作手册。

- **一键启动脚本（根目录）**：
  - `start.bat`（ASCII/CRLF 壳，双击入口）+ `start-hercules.ps1`（UTF-8 BOM，中文逻辑）：检查 Java/8080 占用 → jar 缺失才打包 → 新窗口启动应用（关窗即停）→ 60s 健康轮询 → 成功横幅 + 自动打开浏览器；
  - 踩坑记录：① `.bat` 内嵌中文 + UTF-8 会被 cmd 按字节错位解析（rem/echo 行被截断为碎片命令）→ 逻辑迁入 ps1、壳保持纯 ASCII；② `write` 工具产出 LF 行尾，批处理必须 CRLF → 显式转换；③ PowerShell 5.1 解析 `.ps1` 需 UTF-8 BOM → 写后补 BOM；④ `timeout` 在无标准输入环境报 "Input redirection not supported"（交互双击不受影响）；
  - 端到端实测通过（跳过打包 → 启动 → 健康就绪 → 浏览器打开课程列表），测试实例已停止。

## 2026-09-05（周四）· 阶段 A+：高可用与容错加固

### 背景

评审发现四个风险点（全部属实）：① Lettuce 未配超时（默认命令超时 60s，Redis 挂起会拖垮 Tomcat 线程池）；② Redis 路径无熔断降级；③ `get(key, loader)` 无击穿防护（热点键过期瞬间并发全量回源）；④ Hikari/MyBatis/事务均无超时传递。

### 完成事项（四道防线）

1. **超时传递**（application.yml）：`spring.data.redis.timeout=200ms`、`connect-timeout=500ms`；HikariCP `maximum-pool-size=20`、`connection-timeout=3000`；MyBatis `default-statement-timeout=3`（秒）；`EnrollmentService` 两事务方法 `@Transactional(timeout = 5)`。
2. **Redis 熔断降级**：新增 `ResilientDistributedCacheManager` 装饰器（Resilience4j 2.4.0，程序化包装）。参数：计数窗口 50、最少 10 次判定、失败率 50%、慢调用阈值 300ms / 占比 80%、OPEN 10s 自动转半开、半开放行 2 次探测。降级语义：get→null（读路径落 DB）、put→跳过、delete→重试 1 次后跳过（残留旧值上界=剩余 TTL，已知取舍）。新增 `isAvailable()` 接口方法供读路径跳过 L2。
3. **击穿防护（两级）**：新增 `SingleFlight`（进程内单飞：ConcurrentHashMap+Future，同键仅一个队长回源，等待者 6s 预算，失败不缓存）+ `CacheLoadLock` 抽象（`RedisCacheLoadLock`：SET NX PX + Lua 比对令牌释放，异常一律按未抢到处理；`NoopCacheLoadLock`：开关关闭/无 Redis 时恒抢到）。`MultiLevelCacheManager` 读路径重构为「L1 → L2 探测（isAvailable 门控）→ 单飞{ 双检 → 分布式锁/轮询 → 安全阀 }」。
4. **可观测**：`CacheStatsCollector` 新增 `redisDegraded` 计数与 `redisCircuitState` 状态（装饰器每次操作后采样）；stats 端点与 Prometheus（`hercules.cache.redis.degraded`、`hercules_cache_redis_circuit_state` gauge）双出口暴露。

### 踩坑记录

- Resilience4j 2.4.0 API 变更：`TaggedCircuitBreakerMeterBinder` 已被 `TaggedCircuitBreakerMetricsPublisher` 取代、`CircuitBreaker.getEventConsumer()` 已不在接口上 → 放弃官方 binder，改为「状态 gauge 直绑 getState() + protect() 调用后采样同步」，零 API 风险；
- 构造器内 `this(...)` 委托参数不能调用实例方法 → `defaultConfig()` 改 static；
- 测试 bug 两处：桩失败次数预留过多导致半开探测仍失败（应为恰好耗尽）；测试 loader 是 Supplier 不能抛 InterruptedException（sleep 需自捕获）。

### 验证

- **单测 46/46 全绿**（cache 21 + sync 19 + application 6；新增熔断状态迁移/慢调用熔断/删除重试/单飞合并/单飞失败重试/锁互斥与容错等 11 个用例）；
- **真机故障演练**（黑洞监听器 6390 模拟 Redis 挂起，应用指向黑洞；不触碰真实 Redis 6379）：
  - 故障期：miss 路径首次 3.4s（建连+命令超时的有界代价）→ 熔断 OPEN 后 **7-8ms**（原方案会是 60s×N 拖垮线程池）；15 同键请求 dbLoad=1（单飞生效）；故障期间选课照常成功（256ms，DB 事实源 + 同步链 put 降级跳过）；
  - 恢复期：黑洞换为 6390→6379 TCP 转发器 → 10s 后半开探测成功 → 两次探测后 **CLOSED**；L2 经真实 Redis 回填（l2Hit=1，8ms 命中），降级计数不再增长。

### 结论

四个风险点全部闭环：最坏路径延迟有界（L1 µs / L2 200ms / DB 3s / 事务 5s），Redis 故障期间系统照常服务（读直连 DB、写不受影响），恢复后自动回切。提交并同步 GitHub。

## 2026-09-05（周四）· 认证系统落地（阶段 A+ 认证）

### 背景

用户提出登录系统设计（双 Token/JWT 规范/BCrypt/黑名单/网关统一鉴权），经分析：整体为正确的业界标准，但需按本项目架构调整——①网关为阶段 H 模块，验签先在应用内、阶段 H 迁网关；②补 5 个缺口：用户表体系（最大缺口）、水平越权收敛（studentId 取自 token，不再信前端传参）、网关透传头防伪造、Redis 故障时的黑名单 fail-open 策略、白名单补 actuator；③IP/UA 绑定延后。

### 完成事项

1. **《认证设计.md》**：双 Token 时序图、Redis 键设计（auth:refresh:{uuid} / auth:blacklist:{jti}）、t_user 表设计、两阶段网关迁移图、故障策略表。
2. **用户体系**：`t_user` 建表（schema.sql）+ `User`/`UserMapper`（hercules-common）+ `AuthUserSeeder` 启动器幂等播种演示账号（BCrypt 密文现场生成，避免静态弱密文；admin/admin123、st001~003/123456）。
3. **双 Token**：`JwtUtil`（jjwt 0.13.0，HS256，payload 仅 userId/username/role/studentId + jti/exp）；`AuthController`（login/refresh 旋转/logout 黑名单）；`AuthStorePort` 抽象 + `RedisAuthStore` 实现 + 测试内存桩（沿用 @Primary 桩模式）。
4. **应用内鉴权**：`SecurityConfig`（无状态、白名单、角色 URL 规则：enrollment→STUDENT、stats/debug→ADMIN、courses→登录即可、其余 denyAll）+ `JwtAuthenticationFilter`（验签→黑名单→SecurityContext，401/403 统一 R JSON）；Redis 故障策略：黑名单查询 fail-open、refresh/登出 fail-closed。
5. **水平越权收敛**：enroll/withdraw 的 studentId 改为取自 JWT claims（请求体收敛为 `{courseId}`），新增 `GET /api/v1/enrollment/mine`；非 STUDENT 角色 → 403。

### 验证

- **单测 53/53 全绿**（新增 AuthFlowTest 6 例：错误密码 401、未认证 401、学生访问管理端点 403、刷新旋转后旧 token 失效、登出黑名单+refresh 吊销、admin 正例；冒烟改造为登录驱动并新增 401 用例）；
- **真机验证**（真实 MySQL/Redis）：st001 登录（STUDENT/20240001/1800s）→ 带 token 查/选课（studentId 服务端自取）→ /mine → 学生访问 stats **403** → 登出后旧 token **401**（黑名单对真实 Redis 生效）→ admin 登录读 stats 正常。

### 修复记录

- `/api/v1/auth/logout` 落入 anyRequest().denyAll() → 403，补 `authenticated()` 规则；
- 演练残留 java 进程锁定 jar 导致 repackage 失败 → 结束进程后重打。

### 结论

认证闭环（登录/刷新/登出/角色控制/水平越权收敛）达成，前端第一期的登录页与请求拦截器可按此契约编码。

## 2026-09-05（周四）· 决策记录：放弃 Docker，全线切换 Windows 原生/嵌入式选型

### 决策

因技术原因本机无法安装 Docker（用户确认），原阶段 B/E/F/H 依赖 Docker 的方案全部调整：

| 阶段 | 原（Docker）方案 | 新方案 | 理由 |
| :--- | :--- | :--- | :--- |
| B 真实同步链 | Canal Server + RocketMQ（容器） | **嵌入式 Binlog 连接器**（mysql-binlog-connector-java 纯 Java 库，随应用运行）→ 复用现有 VersionChangeConsumer | 零中间件、Windows 可运行；「外部直改库同步缓存」演示点保留；单节点顺序性由单消费者串行保证 |
| E RAG | Milvus(+etcd/minio) + Neo4j（容器） | **Spring AI 嵌入式向量检索**（JSON 落盘，50 门课规模足够）+ **Neo4j Windows 原生 zip** | Milvus 无官方 Windows 支持；预留 MilvusVectorStore 扩展接口 |
| F 可观测 | Prometheus + Tempo + Grafana（容器） | **Prometheus/Grafana Windows 原生包** + **Zipkin 可执行 jar**（micrometer-tracing 官方对接） | 全部免容器；Tempo+OTel Collector 让位于 Zipkin 单 jar |
| H 部署 | docker-compose 容器化 | **Windows 原生多进程部署**（start.bat 风格，可选 nssm 服务化） | K8s/Beyla 保持论文展望 |

起步文档（含 Docker Compose/K8s 章节）作为历史基线**不修改**；论文在「相关技术/选型对比」章节呈现 Canal/RocketMQ vs 嵌入式 Binlog 连接器、Milvus vs 嵌入式检索的取舍论证。

### 文档同步

- 《环境配置与部署.md》：删除 Docker 章节，重写为「多进程原生部署」规划（组件形态表：嵌入式库/Windows 原生 exe/zip/jar）；模式一表格 Docker 行改为「不需要」；选课冒烟脚本同步认证后的请求体变更（courseId）；
- README：路线图阶段 B/E/F/H 描述与技术栈表规划行同步更新；
- 受影响的前端设计/认证设计/测试报告无 Docker 依赖，不变。

### 后续排期（更新后）

阶段 B（嵌入式 Binlog 同步链，约 1-1.5 天）→ C 巡检（1 周）→ D 智能体（1.5-2 周，OpenAI 兼容 Key）→ E RAG（嵌入式向量+Neo4j 原生，1-1.5 周）→ F 可观测（0.5-1 周）→ G 前端（2 周，可并行）→ H 网关+压测+原生部署（1 周）。**阶段 B 不再有任何外部依赖，可立即开工。**

### 补充产出

- **《开发文档/测试报告.md》**：汇总自动化测试（46/46，按模块明细）、真机功能冒烟（六步演示）、故障演练（熔断/击穿/恢复全链路观测数据）与遗留问题清单，结论为测试通过。
- **《开发文档/前端设计.md》**（用户决策：前端提前开工，先设计后编码；Node v24.9 / npm 11.12 已就绪）——场景驱动用户旅程（S1-S4，journey 图×2）、三层组件体系与复用矩阵（CourseCard 三处复用 / StatCard 六宫格等，graph TD 依赖图）、路由表与守卫约定（graph TD 路由树）、Pinia 领域分片 5 store + 单向数据流五条铁律（flowchart LR + classDiagram）、API 层 `R<T>` 解包约定与目录树。**文档先行，hercules-ui 编码待用户评审设计后开工**；对话界面本期仅壳子（mock），阶段 D 接 SSE+LLM，登录占位阶段 H 接 JWT。

## 2026-09-07（周一）· 第二次决策反转 + WSL2/Docker 部署与最小网关（阶段 H-1~H-3）

### 决策反转

用户找到 WSL2 + Ubuntu 24.04 的安装方法 → **Docker 回归**（上一日的「放弃 Docker」决策作废），且选型**全面恢复起步文档口径**：B=Canal+RocketMQ、E=Milvus+Neo4j、F=Prometheus+Tempo+Grafana（全部容器化）；嵌入式方案降级为「单机形态」备选。用户另拍板：压测含最小网关、阶梯加压至 500。昨日文档中的选型记录保留（嵌入式/原生部署作为单机形态附录与论文对比素材），今日按 Docker 口径更新。

### 完成事项（阶段 H-1~H-3）

1. **环境验证（H-1）**：WSL2 + Ubuntu-24.04 已运行（systemd 启用）；Docker Desktop 用户级安装于 `%LOCALAPPDATA%\Programs\DockerDesktop`，Windows docker CLI 29.7.2 + compose v5.5.0 可用；排障记录：①守护进程未运行 → 启动 Docker Desktop 后就绪；②Windows CLI 首次不在 PATH（用户级安装路径，新进程已可见）；③WSL 集成 toggle 时序问题——**统一改用 Windows CLI 操作，绕过 WSL 集成**。
2. **最小网关（H-2）**：`hercules-gateway`（Spring Cloud 2025.0.3 + `spring-cloud-starter-gateway-server-webflux`，WebFlux 独立进程）：路由 `/api/**` → 应用；`JwtGatewayFilter`：白名单 → 剥离伪造 X-Auth-* 头 → 验签/过期 → 注入 `X-Auth-UserId/Role` 透传头；与后端共享密钥（双层校验，后端 JWT 过滤器保留）；黑名单网关层查询暂缓（应用层兜底）。
3. **Docker 部署编排（H-3）**：`deploy/docker-compose.yml`（mysql:8.0 ROW binlog / redis:7 / app 镜像 / gateway 镜像 / prometheus / grafana，healthcheck 依赖链）+ `Dockerfile-app/-gateway`（temurin 21-jre + Windows 构建 jar COPY）+ Prometheus 抓取配置；**镜像加速前缀 `${DOCKER_MIRROR:-docker.m.daocloud.io}`** 可换源（应对 Docker Hub 直连阻断，已实测生效）。

### 修复记录

- 网关缺 `JwtUtil` @Bean → 启动失败（UnsatisfiedDependencyException），补 Bean 后恢复；
- Docker Hub 直连阻断（dial tcp registry-1.docker.io 超时）→ 按约定停下报告 → 用户选镜像加速前缀方案 → daocloud 前缀实测拉取成功；
- 演练残留 java 进程锁定 jar → 结束后重打包。

### 验证（真机全栈）

- 经网关（localhost:8081）：st001 登录（STUDENT/20240001）→ 查课程 → 选课（studentId 取自 token）→ 学生访问 stats **403** → admin 读统计（applied=1, circuit=CLOSED）；
- Prometheus：hercules-app **up**、hercules-gateway **up**；直连应用 8080 UP；Grafana 3000 就绪；
- 后端代码零改动（双层校验防御纵深，透传头供未来多服务扩展）。

### 下一步

阶段 H-4：JMeter 阶梯压测（100→300→500，Windows JMeter → 网关 8081，含 401 静默刷新场景）→ H-5 报告与文档 → 阶段 B（Canal+RocketMQ 容器版）。

### 压测准备（H-4 前置）：JMeter 引入 + 数据放大

1. **JMeter 经 Maven 引入**：新建独立工程 `loadtest/`（jmeter-maven-plugin 3.8.0），`mvnw -f loadtest/pom.xml jmeter:configure` 从 Central 组装 **JMeter 5.6.2** 运行时到 target（插件会话目录；headless 执行无需 bin 脚本，GUI 调试如需再补官方 zip）；插件默认核心 5.6.2，满足起步文档 5.6+ 口径；HTTP 采样器构件 ApacheJMeter_http 已声明；JVM 堆 2g（500 线程）。
2. **数据放大**（用户指出 50 门课对压测过小——全量可被缓存装下，压测退化为纯缓存测试，且 500 写并发砸 50 行会使行锁等待扭曲结果）：
   - `AuthUserSeeder` 账号数参数化（`hercules.auth.demo-student-count`，默认 3）：st001~st{N} 连续编号；压测环境 **60 个登录账号**；
   - 新增 `LoadTestSeeder`（`hercules.loadtest.seed` 开关默认关闭）：JdbcTemplate 批量插入压测课程（LOADTEST-* 前缀，INSERT IGNORE 幂等可续播），压测环境补至 **5000 门**（容量/已选数随机分布，写并发行锁均匀分散）；
   - compose 注入：demo-student-count=60、loadtest.seed=true。
3. **验证**：容器内 MySQL 实测 courses=**5050**（50 演示 + 5000 压测）、users=**61**（1 admin + 60 学生）；st001/st060 登录通过；深分页（page=90）与压测课程详情读取正常。
4. **发现与记录**：Spring Boot 的 ApplicationRunner 在 web 容器启动后执行——健康检查就绪时 Runner 可能仍在播种（readiness 语义注意）；JDBC 批量插入未开 `rewriteBatchedStatements` 时 5000 行约 57s（可优化项，记录不阻塞）。

### 压测可视化（H-4 前置）

1. **Grafana 实时大盘**：`deploy/grafana/` 自动装配（数据源 Prometheus + 大盘 JSON），compose 挂载生效；大盘「Hercules 压测总览」8 面板：应用/网关 QPS、P95/P99 延迟、4xx/5xx 错误率、L1/L2 命中与回源、同步链 applied/conflict、Redis 降级与熔断状态、JVM 堆、Tomcat 线程；压测时 5s 刷新实时观测阶梯爬升。入口 `http://localhost:3000`（admin/admin123）。
2. **应用侧 P95 数据源**：开启 `management.metrics.distribution.percentiles-histogram.http.server.requests`（直方图懒创建——重启后需有请求才有序列，验证时注意）。
3. **JMeter HTML 报告**：loadtest 插件 `generateReports=true`，每次 run 自动生成时序可视化报告（TPS/分位/错误分布，论文素材）。
4. **验证**：Grafana 数据源/大盘装配查询确认；应用直方图序列在触发请求后出现；Prometheus 查询 `hercules_cache_redis_circuit_state` 返回正常。

## 2026-09-07（周一）· 日志设计与实施（阶段 A+ 日志设计）

### 背景

评审结论：项目有「日志实践」（零散前缀约定）而无「日志设计」——无 traceId 贯通、无文件滚动、无认证审计、中文编码未声明、敏感信息无成文约束。

### 完成事项

1. **logback-spring.xml ×2**（application/gateway）：控制台 + 滚动文件（按天+100MB，保留 30 天/1-2GB，UTF-8 显式声明）；应用主文件经 **AsyncAppender**（队列 4096、neverBlock）——压测期不阻塞业务线程；认证审计独立 appender（auth-audit.log，保留 90 天）；容器内路径 `/app/logs` 并挂载 named volume 持久化。
2. **traceId 贯通**：网关 filter 取/生成（16 位短 ID，复用上游 X-Trace-Id）→ 注入下游请求头与响应头 → 应用 `web/TraceIdFilter`（FilterRegistrationBean，HIGHEST_PRECEDENCE，先于 Security 链）写 MDC → 日志 pattern `%X{traceId}` 输出；AFTER_COMMIT 同步链同线程执行，消费日志天然携带。
3. **认证审计**：专用 logger `AUTH_AUDIT`（独立落盘 + 控制台）：LOGIN_SUCCESS / LOGIN_FAILURE（username+ip+reason，**不含密码**）/ LOGOUT / LOGOUT_FAILED / TOKEN_REJECTED_BLACKLIST（过滤器层）；客户端 IP 取网关注入的 X-Client-IP 回退 remoteAddr。
4. **敏感信息规范成文**（《日志设计.md》红线节）：password/token/secret/SQL 参数一律不落日志。
5. **《日志设计.md》**：格式/文件布局表、traceId 时序 Mermaid、审计事件表、级别与前缀约定、敏感信息红线、压测与容器注意、排查动线（响应头 traceId → grep 两端日志）。

### 验证

- **回归 53/53 全绿**；冒烟新增断言：响应头携带 X-Trace-Id；
- **真机容器验证**：`/app/logs/auth-audit.log` 落盘且含 `LOGIN_SUCCESS username=st001 ip=... [traceId]`；选课请求的 traceId 在应用日志同步链 applied 行**精确出现 1 次**（网关→应用→同步链贯通）；响应头单值无重复（修复网关与应用双重注入）。

### 修复记录

- 网关 traceId 变量因重赋值失去 effectively-final → lambda 编译失败，改为单次赋值表达式；
- 网关与应用对响应头双重注入导致值重复 → 网关仅在拒绝路径自行添加，透传路径由应用回写。

### 结论

日志设计闭环（格式/滚动/审计/traceId/敏感红线），压测期日志高并发不阻塞、容器重启不丢日志，故障可按 traceId 秒级定位。

## 2026-09-07（周一）· 阶段 H-4 压测：JMeter 调试修复 + 500 并发阶梯压测

### 背景

压测脚本连番受阻：setup 登录 200 但所有压测请求 401；部分线程 StackOverflowError 死亡；
线程组 0 线程或线程一轮即停。用户要求"出现问题停下汇报"，逐层定位后按"无聊计划"原则重构。

### 排查与根因（按时间线）

1. **`jmeter:run` 目标不存在** → 插件 3.8.0 实为 `jmeter:jmeter`，且需先 `jmeter:configure`；
2. **JMX ClassCastException**（RegexExtractor 后缺 `<hashTree/>`）→ JMX 规范：每个元素后必须跟（可为空的）hashTree；
3. **setup 登录 415→500** → setup 采样器缺 Content-Type 头（JMX 补齐 + 后端补 415 处理器）；
4. **读日志管道 bug 误导两轮排查**：字符串数组取 `$_.Line` 输出恒空 → 改用 `Select-String`/`$_` 直读后立即抓到真凶；
5. **StackOverflowError 根因**：Once Only Controller + 配置元素组合，每次迭代开始 `recoverRunningVersion` → `hashCode` 无限递归（调用栈实证 `TestElementProperty.hashCode` 循环），线程死亡、采样停止；
6. **401 根因**：Header Manager 的线程变量 `${token}` 求值时机不稳——setup 已提取合法 JWT（len=304，日志实证）但主线程请求头仍为空，网关验签失败；
7. **`-J` 传参字面量**：`-Jthreads=$Threads` 直达 JMeter 未展开（jmeter.log 实证）→ 0 线程；改为参数数组回显 + 全参数走 `-q` 属性文件；
8. **线程一轮即停**：每线程恰 3 采样（1 读迭代 + 1 写迭代）后退出、测试在 rampup 结束时提前收尾——嫌疑锁定 ThroughputController perThread 语义（翻转 continue_forever 无效）。

### 方案重构（"无聊计划"原则，见测试报告 §6.4）

- **删除**：setup 线程组、Once Only、BeanShell、ThroughputController——全部冷门组合清除；
- **JMX 仅剩**：双线程组（读 70% / 写 30%，用线程组规模建模流量比例）+ TestPlan 级 HeaderManager + 3 个 HTTP 采样器，计划内零脚本；
- **token**：登录移交 `run-loadtest.ps1`（Invoke-RestMethod，每级重新登录规避 30min 过期），写属性文件，Header 读 `Bearer ${__P(token)}`；
- **看门狗**：`Start-Process -PassThru` + `WaitForExit(rampup+duration+180s)` 超时强杀，JTL 已落盘可 `-g` 重生成报告。

### 结果（分端点明细见测试报告 §六）

1. **冒烟**：5 线程 728/s、20 线程 3,470/s，均 0 错误——重构后一次通过，此前全部卡点消失；
2. **阶梯**：100 并发 4,051/s（0 错）→ 300 并发 4,630/s（0 错）→ 500 并发 **5,027/s**（3,018,549 请求，20 错 = 0.0007%）；
3. **写路径 20 个 500 定位**：`HikariPool-1 - Connection is not available, request timed out after 3000ms (total=20, active=20, idle=0, waiting=116)`——连接池饱和，3s 获取超时快速失败（阶段 A+ 全链路超时按设计生效，无堆积无扩散）；
4. **读路径 0 错误**：约 4,800 读/s 下 P95 ≤126ms，多级缓存满压不劣化；
5. 同步完成：部署文档 §二 从过时的"原生部署"改回 WSL2 Docker 五容器形态（含压测使用说明）。

### 结论

压测准入通过。全部卡点源于 JMeter 冷门结构而非服务端——"最常用元素 + 逻辑外移启动脚本"后一次通过；
写路径连接池瓶颈与阶段 B（RocketMQ 异步化削峰）形成自然衔接，作为论文瓶颈分析素材。
