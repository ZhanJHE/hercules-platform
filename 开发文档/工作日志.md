# 工作日志

> **项目**：智慧校园多智能体服务平台（Hercules）——MVP：分布式缓存一致性治理核心
> **记录规则**：按日期条目记录当日完成事项、遇到的问题与处置、产出物清单；「已知事项」小节累积备注（答辩备查 / 后续待办）。

---

## 2026-09-02（周三）

### 1. 起步文档研读与 MVP 方案规划

- 通读《起步文档.md》（12 周迭代计划、四层架构、技术选型、数据库设计、风险清单）与《设计类.md》（Java 类架构蓝图：包结构树 + 7 张 mermaid 类图）。
- 核实本机环境：JDK 25（编译目标仍设 21）；**MySQL 8（3306）与 Redis（6379）已在本机运行**；Docker 未安装；Maven 未安装（使用 wrapper 3.9.16）；`~/.m2` 无镜像配置（直连中央仓库），本地仓库已有 531MB 缓存。
- 与项目所有者确认三项决策：
  1. **MVP 范围 = 纵向切片**：课程查询走多级缓存 + 选课写库 + 进程内向量时钟同步链；不引入 Canal/RocketMQ/LLM；
  2. **Spring Boot 骨架降级**：4.1.1 → 3.5.16（与起步文档选型生态 MyBatis-Plus / Spring AI Alibaba 兼容）；
  3. **单模块分包起步**：`com.zhanjh.hercules.*` 同构于目标多模块，Sprint 2 再拆分。
- 产出《最小可执行方案》：7 步实施、6 条验收标准（DoD）、预估 4~4.5 人日。

### 2. MVP 开发（纵向切片）

- **构建前提**：核实 Spring Boot 3.5.x 最新补丁 3.5.16、MyBatis-Plus 3.5.17 均存在于 Maven Central。
- **工程与配置**：pom 降级并补齐依赖（web/validation/actuator/data-redis/mybatis-plus/mysql-connector-j/caffeine/micrometer-registry-prometheus/test+h2）；`application.yml` + `application-local.yml`（真实凭据，已 gitignore）；`schema.sql`（t_course / t_enrollment / t_cache_version 建表 + t_repair_log / t_agent_trace 占位表）；`data.sql`（50 门课程、20 名教师种子，幂等 INSERT IGNORE）。
- **缓存内核**（对应 FR-HC-01）：`MultiLevelCacheManager`（L1 Caffeine → L2 Redis → MySQL，cache-aside 逐级回填，L2 允许缺省）、`FixedTTLStrategy`（列表键 10s 最终一致窗口 / 详情键 L1 60s、L2 300s）、`CacheStatsCollector`（命中统计 + Micrometer FunctionCounter 指标族）。
- **向量时钟同步链**（对应 FR-HC-03，核心创新点）：`VectorClock`（increment / merge / compare / isConcurrent，TreeMap + synchronized）；`VersionedValue` 载荷；进程内事件总线 `InProcessEventBusProducer` + `@TransactionalEventListener(AFTER_COMMIT)` 保证「先提交、后同步」；`VersionChangeConsumer` 决策表——AFTER 应用（写 L2 + 失效 L1 + upsert 版本记录）、EQUAL/BEFORE 幂等丢弃、CONCURRENT 走 `FieldLwwMergeStrategy` 字段级 LWW 合并；向量时钟持久化到 `t_cache_version.vector_clock_json`。
- **API 层**：课程分页/详情、选课（事务内防超选条件 UPDATE）/退课、缓存统计、答辩演示钩子（`POST /api/v1/debug/simulate-conflict`、`/evict-local`）。
- **中途修复的代码问题**：
  1. MyBatis-Plus 3.5.9+ 将分页插件拆分至独立模块 → 补 `mybatis-plus-jsqlparser` 依赖（编译错误消除）；
  2. 测试类型与断言问题 2 处（cacheHitRate 需 double 断言；`l2Miss` 两次未命中应计 2）。

### 3. 构建与验证

- `mvnw test`：**31/31 全绿**（VectorClock 9、FieldLwwMergeStrategy 5、MultiLevelCacheManager 6、VersionChangeConsumer 5、端到端冒烟 6——含防超选 409 与并发合并断言）。
- 真机运行冒烟（连本机 MySQL + Redis，全程无 Docker），演示脚本六步全部符合设计：
  1. 列表首次 `dbLoad=1` → 第二次 `l1Hit=1`；
  2. 失效 L1 → `l2Hit=1` 回填、`dbLoad` 不变；
  3. 选课 enrolled 87→88（事务提交后同步链自动刷缓存）；
  4. node-sim 并发冲突合并：courseName 更新、enrolled 保留，`conflict=1`；
  5. 退课产生支配版本覆盖合并值（87、原名）；
  6. `/actuator/prometheus` 暴露 `hercules_cache_*` / `hercules_sync_*` 指标。
- **环境问题与处置**（按约定：停下询问/系统排查，不盲目重试）：
  1. Maven wrapper 首次运行需写 `~/.m2/wrapper/dists`，被工作区写沙箱拒绝 → 按沙箱规则一次性提权重试，经所有者审批后通过；
  2. `Start-Process` 启动的应用进程随前台命令结束被沙箱回收（health 曾短暂 UP 后连接被拒）→ 改用**后台任务**常驻运行，验证完成后 `job_kill` 停止；
  3. mermaid.ink 渲染返回 400 → 编写探针脚本对比 4 种编码格式：原始代码 base64 ✅、JSON state base64 ✅、deflateRaw/pako ❌（400），据此修正渲染脚本（环境连通正常，属请求格式问题）。

### 4. Javadoc 中文注释工程（五路并行，进行中 → 已完成，见补记）

- 统一规范：类/接口/枚举/Record 顶部 Javadoc（首句定位 + `<p>` 分段机制说明 + `@author zhanjh` + `@since 0.0.1`）；方法级 `@param/@return/@throws`；逐字段注释（业务含义、默认值、对应表列/配置键）；行内 `//` 注释解释「为什么」；**只允许改注释、严禁改代码**。
- 分工与进度：sync（12 文件）✅、common/model/mapper（12 文件）✅、service/controller/config（8 文件）✅、cache（11 文件）✅（补记）、test（6 文件）✅（补记），共 **49 文件**。
- 各路均回报「代码零改动」核验方式（剥离注释逐行比对 / 逐行抽查）。
- 完成后统一执行 `mvnw test` 回归（结果见补记）。

### 5. 开发文档归档

- 新建 `开发文档/` 目录；`起步文档.md` 移入归档。
- `images/`：7 张类图 PNG（`extract_mermaid.py` 渲染，Python 3.11.9 标准库实现，经 mermaid.ink）——领域模型 / 缓存治理层 / 多智能体决策层 / RAG 检索层 / 巡检自愈引擎 / 可观测性 / API 网关层。
- 《包结构说明.md》：整体包结构树 + 11 个模块逐类详细注释（含 MVP 实现状态标注 ✅/🔄/⏳）、模块依赖矩阵、MVP 单模块 → 目标多模块映射表。
- 本日志《工作日志.md》建立。

### 已知事项与后续待办（注释工程发现汇总，答辩备查）

> 来源：五路注释任务的「只报告不修改」清单，按主题归并。

**API/校验类**
1. `@NotNull` 校验实际不生效：控制器参数未加 `@Valid`，缺 courseId 表现为 404「课程不存在： null」而非 400；缺 studentId 会落库 null（Sprint 2 收紧）→ **当日已修复**（见补记）。
2. 无重复选课约束：应用层不查已有 status=1 记录，`t_enrollment` 亦无唯一约束，可重复选课致 enrolled 与记录数失衡。
3. 列表缓存键未转义：keyword 含 `:` 时键段可能碰撞；"Java"/"java" 键分裂；page/size 无边界校验（size 大值可单页拉全表）→ **键碰撞当日已修复**（百分号转义）；大小写分裂与分页边界保留（不影响正确性）。
4. 500 响应携带原始异常消息，可能泄漏内部细节，上线前收紧。

**演示注意类**
5. 连续两次 `simulate-conflict`：第二次 `{node-sim:1}` 相对存量时钟为 BEFORE 被幂等丢弃——答辩演示前需先在 node-1 发生一次选课/退课。
6. `simulate-conflict` 不写 `t_course`，TTL 过期回源后课程名还原（演示窗口期注意）。

**并发/一致性类**
7. `CacheVersion` upsert 为 select-then-write 无锁：单实例（进程内总线串行驱动）安全；多实例需乐观锁或 `cache_key` 唯一键冲突重试。
8. `FieldLwwMergeStrategy` 为「字段并集 + LWW」叠加语义：新值时间戳更旧时，仅新值存在的字段仍会并入（严格 LWW 应整体保留旧值）——答辩追问时需备好说辞。
9. 冲突合并的旧值取自 L2：L2 被逐出时退化为整体采用新值，仅存于旧值的字段丢失（冲突路径已知盲区）。

**数据库类**
10. `enrolled`/`capacity` 仅靠条件 UPDATE + 应用层校验，无 DB CHECK 兜底。
11. JSON 列以 TEXT 承载（与起步文档 JSON 类型有偏差，Sprint 2 如需 JSON 函数再切换）。

**代码卫生类**
12. `VectorClock.fromJson` 的 merge 函数为防御性死代码（Jackson 解析阶段重复 key 已被后者覆盖）。
13. `STATUS_PRESELECT` 为无写路径的预留常量；`withdraw` 的 409 分支正常数据不可达（防御代码）。
14. `HerculesSyncProperties.maxNodes` 无下限校验；事件监听器对 null 载荷兜底不闭合。
15. **L1 列表键短 TTL 不生效**（cache 注释任务发现）：Caffeine 原实现忽略逐键 TTL 参数，列表键在 L1 按全局 60s 过期，「10s 最终一致窗口」仅 L2 生效 → **当日已修复**（自定义 Expiry，见补记）。
16. 测试健壮性备注：`concurrentVersionIsMergedWithLww` 的 LWW 胜负依赖系统时钟不回拨（正常恒成立）；`readLong` 的 JSON Pointer 转换（`"$.x"`→`"/x"`）经实测行为正确。

---

### 补记（当日 15:10~16:00，阶段 0 完成）

- Javadoc 注释工程五路全部完成（49 文件），统一 `mvnw clean test` 回归：**31/31 通过**，确认注释未破坏任何代码。
- 阶段 0 三项修复（对应已知事项 #1、#3、#15）：
  1. 控制器请求体补 `@Valid`，`@NotNull` 生效（字段缺失返回 400 而非 404/null 落库）；
  2. `CacheKeys.courseList` 关键词百分号转义（`%`→`%25`、`:`→`%3A`），杜绝列表键键段碰撞；
  3. `CaffeineLocalCacheManager` 重写为自定义 Expiry 逐键 TTL——**L1 的列表键 10s 最终一致窗口自此生效**；修复过程中测试抓出 `expireAfterRead` 返回 `Long.MAX_VALUE` 会被 Caffeine 理解为「读一次即永生」的语义错误，改为返回剩余时长（读不续期）。
- 修复后回归：**35/35 通过**（新增 `CaffeineLocalCacheManagerTest` 4 例）。
- 本地 git 仓库建立于工作区根（纯本地、无远端），完成基线提交；后续每阶段一个 commit。
- 下一阶段：多模块化重构（阶段 A）已获批启动。

## 2026-09-02（周三）· 阶段 A：多模块化重构

### 完成事项

1. **多模块骨架**：新建 `hercules-platform/`（父 POM 继承 spring-boot-starter-parent 3.5.16 + dependencyManagement 统一内部模块/mybatis-plus 版本），11 个模块：common / cache / sync / inspector / rag / agent / observability / admin / application / gateway（gateway 为独立进程，Spring Cloud BOM 预留注释，阶段 H 启用）。
2. **代码平移**（按包 1:1）：common+model+mapper → `hercules-common`；cache → `hercules-cache`；sync → `hercules-sync`；service/controller/config/启动类 → `hercules-application`（唯一可执行 jar）；resources（yml/schema/data/凭据）全部归 application。
3. **模块解耦修正**：
   - `GlobalExceptionHandler` 从 common 调整为应用层 `com.zhanjh.hercules.web` 包（common 不再依赖 spring-web），补显式 import；
   - `HerculesSyncProperties` 改由 sync 模块新增 `SyncConfig` 自注册，`CacheConfig` 不再跨模块 enable（消除 common←cache←sync 之外的耦合）；
   - 测试桩 `InMemoryDistributedCacheManager` 按需复制到 cache/sync/application 三个测试树（避免引入 test-fixtures 模块的额外复杂度，MVP 取舍）。
4. **构建验证**：reactor 全绿——cache 10、sync 19、application 6（H2 冒烟），共 **35/35**；`mvnw package` 产出可执行 jar（47MB）。
5. **真机冒烟复验**（连本机 MySQL/Redis）：列表 50 条；选课 course2 95→96（同步链生效）；node-sim 冲突合并（名称合并、enrolled 保留，conflict=1）；**@Valid 生效——缺 studentId 返回 400**（阶段 0 修复项真机确认）；退课回落 95。
6. **退役与归档**：旧 `hercules/` 目录删除（git 基线 `bfb4b6e` 保留历史）；README 重写迁移至 `hercules-platform/README.md`；仓库根新增 .gitignore（target/node_modules/凭据等）；Maven wrapper 复制到平台根。

### 本阶段修复的构建问题

- sync 测试缺 `InMemoryDistributedCacheManager`（平移时漏复制一份）；
- `GlobalExceptionHandler` 换包后 `R`/`BusinessException` 需显式 import（原同包免导入）。

### 结论

多模块化完成，功能与单模块版完全等价；后续阶段（B 真实同步链路 → C 巡检 → D 智能体 → E RAG → F 可观测 → G 前端 → H 网关/压测/部署）在对应空模块内填充即可，不动既有模块结构。
